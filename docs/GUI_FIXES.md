# GUI fixes

- Fixed Level View crash when the selected view is `game`.
- `game` view now has its own info panel path instead of being parsed as `logic*`.
- Powerup metadata is counted in the info panel but not drawn in the normal gameplay view.
